{
  "configurations": [
    {
      "name": "Python Debugger",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",   // roda o arquivo atual
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/local_setup/src"
      }
    }
  ]
}
